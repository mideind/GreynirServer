# NOTE: dependencies are now declared in pyproject.toml, and CI installs from
# uv.lock. This file is kept only because scripts/deploy.sh still does
# `pip install -r requirements.txt` against the production venv, which is not
# yet on PyPy 3.11. Keep it in sync with pyproject.toml by hand until deploy.sh
# moves to `uv sync`, at which point this file should be deleted.

# Flask
werkzeug>=2.3.7
Flask==2.3.3
flask-caching>=2.3.1
Flask-Cors>=5.0.1
# DB
psycopg2cffi==2.9.0
SQLAlchemy==1.4.54
sqlalchemy2-stubs>=0.0.2a38
sqlalchemy-stubs>=0.4
# Server
gevent>=24.2.1
setuptools>=67
# Util
cachetools>=5.5.2
requests>=2.32.3
typing-extensions>=4.13.2
python-dotenv>=1.1.0
# Scraping
beautifulsoup4==4.13.4
feedparser==6.0.11
# Query modules
pytz==2025.2
timezonefinder==6.5.9
rjsmin>=1.2.4
python-youtube==0.9.1
country-list>=1.1.0
# For OpenAI GPT support
openai>=1.44
httpx>=0.27.2
# Ours
reynir>=3.5.7
islenska>=1.0.3
# Tracked from git rather than PyPI: the latest release (0.3.7) still pins
# pydantic==2.3.0, which cannot be installed on Python 3.13+. Switch this to a
# plain version specifier once 0.3.8 is on PyPI.
icespeak @ git+https://github.com/mideind/Icespeak@master
iceaddr>=0.5.8
iceweather>=0.2.3
# >=0.1.3 dropped the pkg_resources import, so setuptools need not be
# held below 82 (which removed pkg_resources) any more.
cityloc>=0.1.3
straeto>=1.4.1

